# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is currently a minimal project containing primarily a static HTML login page in the `sketch/` directory. The project appears to be in its early stages.

## Current Structure

```
/data/projects/unmapped/
├── README.md                    # Empty placeholder
└── sketch/
    └── login.html               # 153-line login page (153KB)
```

## Login Page Details

The `sketch/login.html` file contains:
- A responsive login UI built with TailwindCSS
- Material Design icons (Material Symbols Outlined)
- Dark/light mode support with custom color scheme
- Custom styling for input autofill
- Minimal JavaScript (none currently functional)
- Contains navigation suppression note related to "Linear/Transactional intent"

## Development Notes

- No build tools, package managers, or test suites currently configured
- No CSS preprocessors or bundlers detected
- The HTML file contains inline TailwindCSS configuration
- Currently a static HTML file with no backend integration

## Future Considerations

- No build or development commands are currently available
- No linting or testing setup exists
- Project structure may evolve as it develops